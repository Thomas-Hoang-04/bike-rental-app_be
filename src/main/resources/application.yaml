spring:
  application:
    name: Bike Rental App

  config:
    import: optional:secrets.yaml

  jpa:
    hibernate:
      ddl-auto: update

    properties:
        hibernate.jdbc.time_zone: UTC

    open-in-view: true

  jackson:
    time-zone: UTC


server:
  port: 4080



